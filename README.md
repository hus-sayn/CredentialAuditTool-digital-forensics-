# CredentialAuditTool-digital-forensics-
A GUI-based Linux credential audit tool in C++/Qt. Retrieves hashed passwords from /etc/shadow (root required), scans directories for possible password leaks, and parses Firefox logins for forensic/security auditing. For educational and authorized testing only.
